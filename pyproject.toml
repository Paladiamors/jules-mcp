[project]
name = "jules-mcp-server"
version = "0.1.0"
description = "MCP server for Google Jules API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "httpx",
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
